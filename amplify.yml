version: 1
backend:
  phases:
    build:
      commands:
        - npm run build
frontend:
  phases:
    build:
      commands:
        -'cd app/compute/default/ && npm install'
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - .npm/**/*
